// Just a quick program that will run one quick function to make sure it's working, 
// allowing me to quickly change things up and test things on the fly.
#include "CacheTable.h"

int main() {
	CacheTable cache;
	cache.loadEntries((char*)"cache-final-state-log.txt");
}
